# drumlab — extra dependencies only (these are what Drumlab itself adds on top
# of torch + demucs + adtof_pytorch). All pure-CPU; none depend on torch.
#
# Drumlab assumes torch/torchaudio are already installed (a GPU build if you
# want CUDA). Install process described in README.md

fastapi
uvicorn
python-multipart
music21
mcp>=1.27,<2
playwright
Pillow
